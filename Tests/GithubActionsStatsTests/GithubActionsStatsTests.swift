import XCTest
@testable import GithubActionsStats

final class GithubActionsStatsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GithubActionsStats().text, "Hello, World!")
    }
}
